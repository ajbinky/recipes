<div class="col s4">
    <a href={{url this._id}}>
        <div class="card">
            <div class="card-image">
                <img src="{{this.image}}">
            </div>
            <div class="card-content">
                <p>{{this.name}}</p>
            </div>
        </div>
    </a>
</div>